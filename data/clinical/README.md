Place the structured clinical datasets here:

- `symptom_disease_prediction.csv`
  Expected shape: binary symptom feature columns plus a disease target column.
- `symptom2disease_specialists.csv`
  Expected columns: a disease column and a specialist/specialty column.

The exact paths are configured in [`config/config.yaml`](/Users/shukansavani/Downloads/app/config/config.yaml).
